import { beforeEach, describe, expect, it } from 'vitest';
import { assessSupportInput } from '@/support/safety';
import { executeSupportModule } from '@/support/execution';
import { listEvidenceJournalEntries, saveEvidenceJournalEntry } from '@/support/persistence/evidenceJournalStore';
describe('Role 4 privacy end-to-end', () => { beforeEach(() => localStorage.clear()); it('blocks high-risk text and isolates journal content by user', async () => { const blocked = await executeSupportModule({ userId: 'a', moduleId: 'support.cognitive_reframing', contextSnapshotId: null, triggerSource: 'manual', selectionMode: 'explicit_request', configuration: {}, metadata: { explicitRequest: 'I want to self harm' } }); expect(blocked.status).toBe('blocked'); expect(JSON.stringify(blocked)).not.toContain('self harm'); saveEvidenceJournalEntry('a', { id: 'one', moduleId: 'support.evidence_journal', category: 'growth', content: 'private', starred: false, retentionMode: 'user_scoped', safetyLevel: 'sensitive', createdAt: '2026-01-01T00:00:00.000Z', updatedAt: '2026-01-01T00:00:00.000Z' }); expect(listEvidenceJournalEntries('b')).toEqual([]); expect(assessSupportInput({ moduleId: 'support.evidence_journal', inputType: 'free_text', text: 'I want to self harm' }).allowed).toBe(false); }); });
